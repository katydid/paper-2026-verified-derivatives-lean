# Std

All these libraries supplement the Lean standard library in a generic way.
They do not contain any specializations for this specific project, even if they contain functions that are only used by this project.